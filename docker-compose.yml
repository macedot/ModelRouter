services:
  ModelRouter:
    #image: ghcr.io/macedot/ModelRouter:latest
    build: .
    container_name: ModelRouter
    restart: unless-stopped
    ports:
      - "12345:12345"
    volumes:
      - ${HOME}/.config/ModelRouter/ModelRouter.json:/app/ModelRouter.json:ro
    environment:
      # Optional: Override log level/format from config
      - OPENMODEL_LOG_LEVEL=info
      - OPENMODEL_LOG_FORMAT=text
    healthcheck:
      test:
        [
          "CMD",
          "wget",
          "--quiet",
          "--tries=1",
          "--spider",
          "http://localhost:12345/health",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
